pre-commit:
  commands:
    check:
      run: npm run biome:check:write
      stage_fixed: true

pre-push:
  commands:
    build:
      run: npm run build
    build-scripts:
      run: npm run build:scripts
