pre-commit:
  commands:
    check:
      run: npx biome check --write .
      stage_fixed: true

pre-push:
  commands:
    build:
      run: npm run build
    build-scripts:
      run: npm run build:scripts
